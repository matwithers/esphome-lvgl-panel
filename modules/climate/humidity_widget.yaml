# Humidity Widget (to be included inside an existing lvgl page widgets list)
#
# vars:
#   row_pos, column_pos, row_span, column_span

container:
    grid_cell_x_align: stretch
    grid_cell_y_align: stretch
    grid_cell_row_pos: ${row_pos}
    grid_cell_column_pos: ${column_pos}
    grid_cell_row_span: ${row_span}
    grid_cell_column_span: ${column_span}
    widgets:
      - obj:
          layout:
            type: flex
            flex_flow: COLUMN
            flex_align_cross: center
            flex_align_track: center
            pad_row: 2

          widgets:
            - label:
                text: "Humidity"

            - label:
                id: humidity_state_indicator
                text: "${mdi_water_percent}"
                text_font: font_mdi_72

            - label:
                id: room_humidity
                text: "==%"
